# Dependencias de PRODUCCIÓN — versiones EXACTAS (==) para reproducibilidad.
# (#FASE2-NEW-2 / BUENAS_PRACTICAS_V2 §7.5). Recrear venv: pip install -r requirements.txt
# Updates: bump manual con validación previa. Dev/test deps → requirements-dev.txt.
alpaca-py==0.43.3
asyncpg==0.31.0
aiohttp==3.13.5
scikit-learn==1.8.0
pandas==3.0.2
python-dotenv==1.2.2
langgraph==1.1.9
fastapi==0.136.1
uvicorn==0.46.0
sse-starlette==3.3.4
authlib==1.7.0
itsdangerous==2.2.0
httpx==0.28.1
anthropic==0.97.0
# Reporte de balance del período de observación (Fase 1).
quantstats==0.0.81
# NOTA (swap FinBERT→DeepSeek): The Ear ya NO usa FinBERT local — torch/transformers
# removidos (aligera el deploy ~5x, era el peso grande del server). Ahora The Ear
# usa Alpaca News (alpaca-py, arriba) + DeepSeek API vía aiohttp (arriba). DeepSeek
# se autentica con DEEPSEEK_API_KEY del .env; no requiere SDK extra.
# Ver the_ear.py / deepseek_client.py.
