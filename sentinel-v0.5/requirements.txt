# Dependencias de PRODUCCIÓN — versiones EXACTAS (==) para reproducibilidad.
# (#FASE2-NEW-2 / BUENAS_PRACTICAS_V2 §7.5). Recrear venv: pip install -r requirements.txt
# Updates: bump manual con validación previa. Dev/test deps → requirements-dev.txt.
alpaca-py==0.43.3
asyncpg==0.31.0
aiohttp==3.13.5
scikit-learn==1.8.0
pandas==3.0.2
python-dotenv==1.2.2
langgraph==1.1.9
fastapi==0.136.1
uvicorn==0.46.0
sse-starlette==3.3.4
authlib==1.7.0
itsdangerous==2.2.0
httpx==0.28.1
anthropic==0.97.0
# Reporte de balance del período de observación (Fase 1).
quantstats==0.0.81
# Sentiment analysis finance-tuned para The Ear (#FEAT-007 / T-U). Se importan
# solo cuando THE_EAR_SENTIMENT_ENABLED=true (lazy load del modelo). CPU build de
# torch obligatorio (la GPU build pesa ~5x y no se usa).
# DRIFT vs spec T-U (forzado por Python 3.14): la spec pedía torch==2.5.0 /
# transformers==4.45.0 — no tienen wheels cp314. Mínimas con wheel cp314:
# torch 2.9.x / transformers 5.x. Modelo: ProsusAI/finbert (yiyanghkust/finbert-tone
# no carga su clasificador en transformers 5.x). Detalle en docs/finbert_recalibration_plan.md.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
transformers==5.9.0
