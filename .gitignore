# === Secretos y credenciales ===
client_secret_*.json
**/.env
**/.env.local
**/.env.production
**/.env.staging
**/.env.bak*
**/*.env.bak*

# === Servidor MCP de coordinación — NO va a GitHub (decisión Roman 31-may) ===
# Todo el sistema de coordinación queda LOCAL, fuera del repo público de ALC.
# Incluye el token en .mcp.json. La durabilidad va por backup de la SQLite, NO por git.
mcp-coordination/
**/.mcp.json
**/mcp_export/
teamwork/mcp_export/
deploy/systemd/mcp-server.service
deploy/systemd/mcp-export.service
deploy/systemd/mcp-export.timer

# === Scripts operativos one-off con PII (owner UUID + emails de cuenta) — NO al repo público (31-may) ===
# Decisión Roman: excluir. Operan sobre la cuenta real (UUIDs/emails), son de un solo uso.
sentinel-v0.5/scripts/persist_residual_closes_*
sentinel-v0.5/scripts/persist_deleverage_*
sentinel-v0.5/scripts/queries_balance_observacion.sql
sentinel-v0.5/scripts/queries_corporate_actions.sql
sentinel-v0.5/scripts/run_persist_*.ps1

# === Entornos Python ===
**/venv/
**/.venv/
**/__pycache__/
**/*.pyc
**/*.pyo
**/.pytest_cache/

# === Logs y bases de datos ===
**/logs/*.log
**/*.log
**/*.db
**/*.dump
**/*.dump.gz

# === Backups in-place: copias de archivos editados (NO van a git) ===
dashboard/*.bak
dashboard/*.bak.*
dashboard/*.backup_*
dashboard/*.pre-*.html
dashboard/*.pre-*.js
dashboard/*.pre-*.py
sentinel-v0.5/*.bak
sentinel-v0.5/*.bak.*
sentinel-v0.5/*.backup_*
sentinel-v0.5/*.pre-*.py
sentinel-v0.5/*.pre-*.js

# === Backups con código completo / dumps / inventarios (subcarpetas) ===
backups/**/*.original
backups/**/*.pre-ronda*
backups/**/*.pre-fix*
backups/**/*.pre-equity-card
backups/**/*.with-cap-total
backups/**/*.tar.gz
backups/**/api.py.*
backups/**/email_service.py.*
backups/**/historian.py.*
backups/**/dispatcher.py.*
backups/**/sentinel-app.js.*
backups/**/sentinel-data.js.*
backups/**/sentinel-i18n.js.*
backups/**/index.html.*
sentinel-v0.5/backups/*.tar.gz
sentinel-v0.5/backups/*.dump
sentinel-v0.5/backups/inventory_*
sentinel-v0.5/inventory_*.txt
backups/**/config.py.*
backups/**/sentinel-v0.5_CLAUDE.md.bak*
# general: cualquier backup de código .py (supera la lista per-file frágil)
backups/**/*.py.*

# === Backups con sufijos de rescue/triage (sesión 24-may incidente Code) ===
backups/**/*.corrupted_*
backups/**/*.uncommitted_*
backups/**/*_pre_rotation
backups/**/*_pre_revert

# === Backups de archivos de configuración del proyecto ===
backups/**/requirements.txt.*
backups/**/requirements-dev.txt.*
backups/**/.env.example.*
backups/**/BUENAS_PRACTICAS_V2.md.*
backups/**/API_REFERENCE.md.*
backups/**/LOG.md.*

# === Backups de scripts PowerShell del proyecto ===
backups/**/*.ps1.*

# === Outputs de datos/reportes (NO son codigo, no van a git) ===
backups/**/*.csv
backups/**/*.html

# === Code outputs (logs de sesiones de Claude Code) ===
code-outputs/

# === Handoffs Design archivados (binarios sin auditar) ===
panel-admin/panel-admin.zip
templetes-correo/templetes-correo.zip

# === Coverage artifacts (pytest-cov, #FASE2-NEW-4) ===
.coverage
.coverage.*
htmlcov/
coverage.xml

# === Backups universales (defensivo, sesión 25-may bundle push 2)
# La lista per-pattern de arriba es histórica y frágil; este blanket
# garantiza que NADA dentro de backups/ se pushea, sin excepciones.
backups/
**/backups/
**/.env.backup*
**/.env.bak*
**/inventory_*.sql
**/inventory_*.txt

# === Reportes/análisis sueltos en root (no del repo)
/investigacion_*.md
/code-outputs/

# === HTMLs viejos #TECH-002 — versiones obsoletas, se quedan locales
# index.html (root) = landing page actual, SÍ se trackea
# index1.html (root) = landing vieja, NO trackear (queda local)
/index1.html
# dashboard/: index.html + admin.html son los ACTIVOS
# Los otros tres son backups/versiones viejas — no trackear
dashboard/index.pre-redesign.html
dashboard/index1.html
dashboard/index2.html
.secrets-patterns.local
